function ph = I_lambda_trajectories(I_lambda1,I_lambda2,varargins)
% I_LAMBDA_TRAJECTORIES - stylish plot(I_1(t),I_2(t)) 
%   Useless function, once uppon a time there was an idea, that
%   idea did not grow up to be a fruitful contributor to science,
%   the end.

% Copyright © 20190506 B. Gustavsson, <bjorn.gustavsson@uit.no>
%   This is free software, GPL version 3 or later applies

ph = plot(I_lambda1,I_lambda2,varargins{:});
